---
# Tasks for Ceph Manager (mgr) setup will be added here.
# This role will depend on ceph_common and ceph_mon (for cluster interaction).
# Responsibilities:
# - Install ceph-mgr package.
# - Manage ceph-mgr service.
# - Manage manager keyring(s).
# - Enable and configure ceph-mgr modules (e.g., dashboard, prometheus).
