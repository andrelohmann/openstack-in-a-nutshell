[global]
fsid = <your_generated_fsid>
mon_initial_members = controller, osd1, osd2  # Hostnames as defined in inventory/DNS
mon_host = 192.168.56.101,192.168.56.102,192.168.56.103 # IPs of MON nodes
public_network = 192.168.56.0/24 # Adapt to your VirtualBox network
cluster_network = 192.168.56.0/24 # Often same as public for simple setups

# Auth settings (defaults are usually fine to start)
auth_cluster_required = cephx
auth_service_required = cephx
auth_client_required = cephx

# Set desired pool defaults
osd_pool_default_size = 2 # IMPORTANT: Set default replication to 2
osd_pool_default_min_size = 1 # Allow writes with 1 replica available (adjust as needed for testing)
# For newer Ceph versions, pg_autoscale might handle PGs, otherwise:
# osd_pool_default_pg_num = 64 # Adjust based on OSD count, start small
# osd_pool_default_pgp_num = 64 # Should match pg_num